# Sentence generation logic for resonance output
return `In the realm of ${data.field}, you move through Gate ${data.gate}.${data.line}, wearing the lens of Color ${data.color}, riding Tone ${data.tone}, and anchoring in Base ${data.base}.`;
